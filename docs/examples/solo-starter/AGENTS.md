# Solo Starter Kit

> **This file has moved.** The canonical version is `AGENTS.md` in the repository root.
>
> Copy `AGENTS.md` from the root of `agentic-dev-framework` into your project. It contains all five agents, the Cross-Agent Coordination section, and the `{{SMOKE_COMMAND}}` token ready for bootstrap resolution.
>
> **Upgrade path:** When you need path-scoped enforcement (security rules, testing gates, individual agent files), copy `.github/` from the framework repo and run `BOOTSTRAP.prompt.md`.
